{
  "name": "nuxt-app",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev -o -p 5777",
    "generate": "nuxt generate",
    "release": "pnpm release:patch",
    "release:patch": "npx changelogen@latest --release --patch --push",
    "release:minor": "npx changelogen@latest --release --minor --push",
    "release:major": "npx changelogen@latest --release --major --push"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "6.13.1",
    "@prisma/client": "6.4.1",
    "@prisma/nuxt": "^0.3.0",
    "nuxt": "^3.15.4",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "prisma": "6.4.1"
  }
}